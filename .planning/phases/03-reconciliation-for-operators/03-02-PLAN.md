---
phase: 03-reconciliation-for-operators
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app.py
  - MEMORY.md
autonomous: true
requirements:
  - REC-01

must_haves:
  truths:
    - Streamlit has a **Reconcile** tab listing scan results and operator-facing documentation
    - Neo4j writes (merges) require explicit acknowledgment + per-merge confirmation
    - Tab uses `run_reconciliation_scan` and `merge_characters` / `merge_entities` from `reconcile.py`
  artifacts:
    - MEMORY.md tab table lists Reconcile
  key_links:
    - Phase 03-01 must land first (scan API + CLI)
---

<objective>
Add a **Reconcile** Streamlit tab that surfaces `run_reconciliation_scan`, documents merge behavior for operators, and performs **confirmed** Character or Location merges via existing `reconcile.py` write APIs. Satisfy REC-01 dashboard path and roadmap dry-run / guardrail criteria.

Output: `app.py`, `MEMORY.md`.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-reconciliation-for-operators/03-CONTEXT.md
@.planning/phases/03-reconciliation-for-operators/03-01-PLAN.md
@app.py
@reconcile.py
@MEMORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Imports and cached scan in app.py</name>
  <files>app.py</files>
  <read_first>app.py (imports, `_neo4j_dashboard_cache_stamp`, tab_labels ~526–540)</read_first>
  <action>Import from **`reconcile`**: `ReconciliationScan`, `merge_characters`, `merge_entities`, `run_reconciliation_scan` (adjust names if 03-01 used aliases—match actual exports).

Add **`@st.cache_data(ttl=120, show_spinner="Scanning graph for reconciliation…")`** function **`_cached_reconciliation_scan(_artifact_stamp: tuple[float, float], min_similarity: float)`** that:
1. Deletes `_artifact_stamp` (same pattern as other cached loaders).
2. Calls **`get_driver()`** from `metrics`, then **`run_reconciliation_scan(drv, min_similarity=min_similarity)`** inside **try** / **finally** `drv.close()`.
3. Wrap the inner call like other dashboard loaders: on failure **`logging.getLogger(__name__).exception`** (or `_log`) and return **`ReconciliationScan(ghost_characters=[], fuzzy_character_pairs=[], fuzzy_location_pairs=[])`** (empty scan—import dataclass if needed for construction).

Use **`_neo4j_dashboard_cache_stamp()`** as the stamp argument at call site (same as dashboard).</action>
  <acceptance_criteria>
    - `rg "run_reconciliation_scan" app.py` matches
    - `rg "_cached_reconciliation_scan" app.py` matches
    - `rg "ReconciliationScan" app.py` matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tab order — insert Reconcile</name>
  <files>app.py</files>
  <read_first>app.py (`_tab_labels`, `st.tabs`, `tab_pipeline` … `tab_investigate`)</read_first>
  <action>Extend `_tab_labels` with **`"Reconcile"`** immediately **after** `"Cleanup Review"` and **before** `"Pipeline Efficiency Tracking"`.

Assign **`tab_reconcile = _tabs[_ti]; _ti += 1`** in the same style as other tabs. Wrap content in **`with tab_reconcile:`** as a new section (placeholder OK until Task 3 fills it).</action>
  <acceptance_criteria>
    - `rg "Reconcile" app.py` matches in `_tab_labels` list
    - `tab_reconcile` variable exists and is used with `with tab_reconcile:`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Reconcile tab UI — docs, scan, tables</name>
  <files>app.py</files>
  <read_first>app.py (new `tab_reconcile` block)</read_first>
  <action>Inside **`with tab_reconcile:`**:
1. **`st.header("Reconcile")`** and a short caption that reconciliation **modifies Neo4j** when merging.
2. **`st.expander("About reconciliation", expanded=False)`** with markdown covering: fuzzy name matching (Character + Location), ghost characters definition (match `find_ghost_characters` docstring intent), merges **keep one `id`** and rewire edges (APOC preferred, manual fallback—operator-safe wording), recommendation to **`--dry-run` CLI** for scripting.
3. **`min_similarity`** via **`st.slider`**, min **0.5**, max **1.0**, default **0.78**, step **0.01**, key **`reconcile_min_sim`**.
4. Compute **`_rec_stamp = _neo4j_dashboard_cache_stamp()`** and **`scan = _cached_reconciliation_scan(_rec_stamp, min_similarity)`** (use slider value).
5. Display **`st.subheader`** + **`st.dataframe`** (or `st.table`) for: **Ghost characters** (columns id, name, scenes, conflicts if present), **Fuzzy character pairs** (ids, names, score), **Fuzzy location pairs** (ids, names, score). Empty lists → **`st.info`** per section.

Use **`pd.DataFrame`** where convenient; **hide_index=True** for dataframes.</action>
  <acceptance_criteria>
    - `rg "About reconciliation" app.py` matches
    - `rg "reconcile_min_sim" app.py` matches
    - At least two `st.subheader` calls in the reconcile tab for scan sections
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Merge guardrails and actions</name>
  <files>app.py</files>
  <read_first>app.py (reconcile tab end), `reconcile.py` (`merge_characters`, `merge_entities` signatures)</read_first>
  <action>Add **`st.divider()`** then subsection **"Apply merge (writes to Neo4j)"**.

1. **`st.warning`** permanent text: merges are **irreversible** in-app; operators should use **dry-run** / backups as appropriate.
2. **`st.checkbox`**, label e.g. **"I understand this will write merges to Neo4j."**, key **`reconcile_ack_write`**.
3. **`entity_kind`** = **`st.radio`**: **`Character pair`** / **`Location pair`**, horizontal.
4. **`st.selectbox`** to pick **pair index** (0..n-1) from the **current scan** list for that kind; show human-readable option labels (e.g. `id_a ↔ id_b (0.82)`).
5. **`st.radio`** **"Keep node id"** with exactly **two** choices: the **two ids** from the selected pair (strings from dicts).
6. **`st.button` "Merge selected pair"**, **`disabled=not acknowledge checkbox`**. On click:
   - Derive **`drop_id`** as the non-kept id.
   - Open **`drv = get_driver()`**, **`try`**: if Character → **`merge_characters(drv, keep_id, drop_id)`**; if Location → **`merge_entities(drv, keep_id, drop_id, "Location")`**.
   - **`finally`**: **`drv.close()`**.
   - On success: **`st.success`**, **`st.cache_data.clear()`** (or targeted clear if you add cache key—full clear acceptable), **`st.rerun()`**.
   - On **`Exception`**: **`st.error`** with safe message (no password leakage).

**Do not** enable merge button without checkbox checked.</action>
  <acceptance_criteria>
    - `rg "reconcile_ack_write" app.py` matches
    - `rg "merge_characters" app.py` matches inside `tab_reconcile` context (file-level grep OK if only reconcile tab calls it)
    - `rg "merge_entities" app.py` matches with `"Location"` nearby or same block
    - `rg "st.cache_data.clear" app.py` exists in merge success path (may match multiple tabs)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: MEMORY.md tab list</name>
  <files>MEMORY.md</files>
  <read_first>MEMORY.md (Dashboard tabs table)</read_first>
  <action>Add a row for **Reconcile**: scan ghosts + fuzzy Character/Location pairs; optional confirmed merges to Neo4j; operator doc in expander. Place the row **after Cleanup Review** to mirror tab order.</action>
  <acceptance_criteria>
    - `rg "\\| \\*\\*Reconcile\\*\\*" MEMORY.md` or `rg "Reconcile" MEMORY.md` matches the tabs table line
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `uv run python -c "import ast; ast.parse(open('app.py').read())"` exits 0
</verification>

<success_criteria>
REC-01 Streamlit path + operator documentation + confirmation/dry-run alignment with roadmap.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reconciliation-for-operators/03-02-SUMMARY.md`
</output>
