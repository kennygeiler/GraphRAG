---
phase: 03-reconciliation-for-operators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reconcile.py
  - README.md
autonomous: true
requirements:
  - REC-01

must_haves:
  truths:
    - `run_reconciliation_scan` (or equivalent name) returns ghosts + fuzzy Character pairs + fuzzy Location pairs without printing
    - CLI supports `--scope all|characters|locations` and documents behavior; `--dry-run` unchanged in spirit
    - README documents how to run reconcile from the terminal
  artifacts:
    - README.md has a Reconciliation subsection with example commands
  key_links:
    - `merge_entities` / `merge_characters` remain the only write paths for merges
---

<objective>
Add a **reusable scan API** to `reconcile.py`, refactor **`main()`** to use it, extend CLI with **`--scope`**, and add **operator documentation** to `README.md` for REC-01 (CLI path).

Output: `reconcile.py`, `README.md`.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-reconciliation-for-operators/03-CONTEXT.md
@.planning/REQUIREMENTS.md
@reconcile.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dataclass (or TypedDict) for scan results</name>
  <files>reconcile.py</files>
  <read_first>reconcile.py (imports through `find_fuzzy_character_pairs`, `fetch_locations`)</read_first>
  <action>Add **`@dataclass`** `ReconciliationScan` in `reconcile.py` with three fields:
- `ghost_characters: list[dict[str, Any]]`
- `fuzzy_character_pairs: list[tuple[dict[str, Any], dict[str, Any], float]]`
- `fuzzy_location_pairs: list[tuple[dict[str, Any], dict[str, Any], float]]`

Use **`from dataclasses import dataclass`** (Python 3.12). Type hints must allow empty lists.</action>
  <acceptance_criteria>
    - `rg "class ReconciliationScan" reconcile.py` matches
    - `rg "fuzzy_location_pairs" reconcile.py` matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: `run_reconciliation_scan` function</name>
  <files>reconcile.py</files>
  <read_first>reconcile.py (`fetch_characters`, `find_ghost_characters`, `find_fuzzy_character_pairs`, `fetch_locations`, `find_fuzzy_duplicate_pairs`)</read_first>
  <action>Add **`def run_reconciliation_scan(driver: Any, *, min_similarity: float = 0.78) -> ReconciliationScan:`** that:
1. Opens **one** `with driver.session() as session:` block (or minimal sessions) to read data.
2. Fetches characters via `fetch_characters(session)`, locations via `fetch_locations(session)`.
3. Sets `ghost_characters = find_ghost_characters(session)`.
4. Sets `fuzzy_character_pairs = find_fuzzy_character_pairs(characters, min_ratio=min_similarity)`.
5. Sets `fuzzy_location_pairs = find_fuzzy_duplicate_pairs(locations, min_ratio=min_similarity)`.
6. Returns **`ReconciliationScan(...)`** — **no printing**, no merges.

Do **not** change fuzzy/ghost query semantics.</action>
  <acceptance_criteria>
    - `rg "def run_reconciliation_scan" reconcile.py` matches
    - `rg "return ReconciliationScan" reconcile.py` matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Refactor `main()` — scope, dry-run, interactive merges</name>
  <files>reconcile.py</files>
  <read_first>reconcile.py (`main` from `argparse` through end)</read_first>
  <action>Add argparse argument **`--scope`** with choices **`all`**, **`characters`**, **`locations`**, default **`all`**.

After `get_driver()` and opening session logic, replace inline fetches with **`scan = run_reconciliation_scan(driver, min_similarity=args.min_similarity)`** (adjust `main` to call scan once with driver — if scan opens its own session internally, `main` should not duplicate fetches; pass `driver` only).

**Printing rules:**
- If scope **`characters`** or **`all`**: print ghost section (from `scan.ghost_characters`) and character fuzzy section (from `scan.fuzzy_character_pairs`) — preserve existing section headers and formatting style where reasonable.
- If scope **`locations`** or **`all`**: print location fuzzy section from `scan.fuzzy_location_pairs` (add a clear header e.g. `=== Fuzzy location name matches ===`).

**`--dry-run`:** Print only; **return before merge tool** (same as today when no pairs or dry-run).

**Interactive merges (not dry-run):**
- When scope is **`characters`** or **`all`**: run existing **per-pair y/n** loop on **`scan.fuzzy_character_pairs`** using `merge_characters` / `_choose_canonical` / `_prompt_yes_no` (same behavior as current CLI).
- When scope is **`locations`** or **`all`**: run an **analogous** loop on **`scan.fuzzy_location_pairs`** using **`merge_entities(driver, keep_id, drop_id, "Location")`** (reuse `_choose_canonical`; on merge failure print error like character branch).

Order: process **character** pair merges first, then **location** pair merges when scope is `all`.</action>
  <acceptance_criteria>
    - `rg "add_argument\\(\"--scope\"" reconcile.py` or `rg "--scope" reconcile.py` matches argparse definition
    - `rg "run_reconciliation_scan" reconcile.py` matches in `main`
    - `rg "merge_entities" reconcile.py` matches a call with `"Location"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: README — Reconciliation section</name>
  <files>README.md</files>
  <read_first>README.md (table of contents, environment / quick start sections)</read_first>
  <action>Add a **## Reconciliation** section (and TOC link under **table of contents**) that explains in plain language:
- Reconcile finds **ghost-like characters** (single scene, no conflicts) and **fuzzy name duplicates** for **Character** and **Location** in Neo4j.
- Merges **keep one node id** and rewire relationships (APOC `mergeNodes` when available, else manual rewire — one sentence).
- Example commands:
  - `uv run python reconcile.py --dry-run`
  - `uv run python reconcile.py --dry-run --scope locations`
  - `uv run python reconcile.py --min-similarity 0.85` (note: interactive merges when not `--dry-run`)

No secrets; Neo4j env vars already documented elsewhere.</action>
  <acceptance_criteria>
    - `rg "## Reconciliation" README.md` matches
    - `README.md` contains the substring `reconcile.py --dry-run`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `uv run python reconcile.py --help` shows `--scope`
- `uv run python -c "from reconcile import run_reconciliation_scan, ReconciliationScan"` succeeds
</verification>

<success_criteria>
CLI path for REC-01 is documented and scan logic is reusable from Streamlit (plan 03-02).
</success_criteria>

<output>
After completion, create `.planning/phases/03-reconciliation-for-operators/03-01-SUMMARY.md`
</output>
