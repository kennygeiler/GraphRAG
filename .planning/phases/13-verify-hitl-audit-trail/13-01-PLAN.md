# Plan 13-01 — HITL-03 Verify audit trail

## Goal

Exportable **decision log** (CSV/JSON) for Verify HITL: decisions, optional per-warning notes, detail text, timestamps; **`neo4j_load_completed_at`** after successful load.

## Tasks

1. **`cleanup_review.py`**
   - `build_verify_audit_payload`, `verify_audit_to_csv`, `verify_audit_to_json`.

2. **`app.py` Verify**
   - Per-card expander **Optional note (audit export)** (`verify_hl_note_{wid}`).
   - Expander **Decision log (audit export)** — download current warnings as CSV/JSON; show last load UTC if set.
   - On **Approve & Load** success: snapshot full warning list + decisions + notes **before** trimming `pr["warnings"]`; store `verify_hitl_load_audit_payload` + `verify_hitl_neo4j_load_at`; extra downloads **last-load** CSV/JSON.
   - Clear audit session keys on new pipeline run and graph reset.

## Verification

- Export includes approve/decline/unset and notes.
- After load, last-load export still lists approved warnings; live export matches trimmed list.
